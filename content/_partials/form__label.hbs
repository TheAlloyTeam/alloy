{{#this}}
	{{#if content.text}}
		{{#if attribute.for}}
			<label
				{{#if attribute.for}}for="{{attribute.for}}"{{/if}}
				class="form__label{{#if attribute.modifier}} form__label--{{attribute.modifier}}{{/if}}
				{{#if attribute.status}} {{attribute.status}}{{/if}}
				{{#if attribute.disabled}} disabled{{/if}}">
				{{#if content.icon}}{{#each content.icon}}{{#this}}{{>icon}}{{/this}}{{/each}}{{/if}}
				<span{{#if attribute.hidden}} class="hidden-text"{{/if}}>{{{content.text}}}</span>
			</label>
		{{else}}
			<div class="form__label{{#if attribute.modifier}} form__label--{{attribute.modifier}}{{/if}}">
				{{#if content.icon}}{{#each content.icon}}{{#this}}{{>icon}}{{/this}}{{/each}}{{/if}}
				<span{{#if attribute.hidden}} class="hidden-text"{{/if}}>{{{content.text}}}</span>
			</div>
		{{/if}}
	{{/if}}
{{/this}}